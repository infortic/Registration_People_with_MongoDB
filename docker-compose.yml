version: "2"
services:
  web:
    container_name: "API Person"
    networks:
      backend:
         ipv4_address: 10.9.0.5
    image: app
    build:
       context: .
       dockerfile: dockerFile
    restart: always
    ports:
      - "5041:5041"
networks:
  backend:
    ipam:
      driver: default
      config:
           - subnet: 10.9.0.0/24